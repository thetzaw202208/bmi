class CartModel{
  String riceType;
  String quantity;
  int totalAmount;
  int orderCount;
  String deliverLocation;

  CartModel(this.riceType, this.quantity, this.totalAmount, this.orderCount,
      this.deliverLocation);
}